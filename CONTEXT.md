# codexCycle

用于让 macOS 用户随时感知 Codex 5 小时与周使用额度剩余情况的状态栏工具。

## Language

**Codex Runtime**:
能代表当前用户提供已认证 Codex 能力与限额数据的本地运行时；它既可以来自独立 Codex CLI，也可以来自受支持的 Codex Desktop。
_Avoid_: Codex CLI（泛指所有运行时来源时）、数据接口

**Codex Desktop**:
当前 ChatGPT 桌面应用中的 Codex 编程体验；codexCycle 对桌面版的正式兼容承诺只指这一当前形态。
_Avoid_: 独立 Codex App、ChatGPT 网页版

**旧版 Codex App**:
Codex 并入 ChatGPT 桌面应用之前发布的独立桌面应用，只接受尽力兼容，不属于 codexCycle 的正式兼容承诺。
_Avoid_: Codex Desktop

**首选限额视图**:
用户希望状态栏指示器优先呈现的限额窗口，可为 5 小时余量或周余量；首次使用时为 5 小时余量，此后沿用用户最后一次选择。即使 5 小时数据当前不可用，用户仍可将其设为首选；每次选择后立即触发一次 Runtime 刷新。
_Avoid_: 默认显示、显示模式

**当前限额视图**:
状态栏指示器此刻实际呈现的限额窗口；通常等于首选限额视图，但首选窗口无可用数据而另一个窗口可用时，对称地回退到可用窗口；首选值保持不变，待后续刷新取得首选窗口数据后自动恢复。用户切换首选视图时立即使用已有读数更新当前视图并标明刷新中，新快照到达后再替换读数。
_Avoid_: 首选限额视图、默认限额

**Codex 5 小时余量**:
主 `codex` 计量桶的 300 分钟限额窗口内尚未使用的额度占比，以 0–100 的整数表示。
_Avoid_: 短期余量、五小时总余量、其他模型余量

**Codex 周余量**:
主 `codex` 计量桶的 7 天限额窗口内尚未使用的额度占比，以 0–100 的整数表示。
_Avoid_: 余量、总余量、五小时余量

**状态栏指示器**:
常驻 macOS 状态栏的圆环与数字，是用户日常查看当前限额视图的唯一常驻显示；不额外承载 `5h` 或“周”等窗口标识。
_Avoid_: 主界面、主窗口

**玻璃表盘**:
状态栏指示器中央承载当前限额视图余量数字的透明圆形区域。
_Avoid_: 背景板、实色圆

**余量环**:
玻璃表盘外缘表示当前限额视图余量的渐变进度线，长度随余量变化。
_Avoid_: 边框、单色状态环

**详情菜单**:
点击状态栏指示器后出现的轻量信息与操作菜单，不构成独立应用窗口。
_Avoid_: 设置页面、详情窗口

**部分可用**:
一次 Runtime 刷新成功且 5 小时或周限额窗口中只有一个可用的状态；可用窗口保持正常展示，缺失窗口不沿用旧读数，也不把整体标记为失败或陈旧。
_Avoid_: 刷新失败、陈旧读数

**陈旧读数**:
某个限额窗口最近一次成功获取的余量；仅在整次刷新失败后可暂时保留并明确标示为非当前数据，且到达该窗口的重置时刻后失效。刷新成功但响应中缺少该窗口时，该窗口属于当前不可用，不得继续使用旧读数。
_Avoid_: 当前余量、实时余量

**充足**:
当前限额视图余量处于 50–100 的状态。
_Avoid_: 绿色状态

**偏低**:
当前限额视图余量处于 20–49 的状态。
_Avoid_: 黄色状态

**紧张**:
当前限额视图余量处于 0–19 的状态。
_Avoid_: 红色状态、耗尽

**重置倒计时**:
从当前时刻到当前限额视图窗口重置时刻的剩余时长，仅以相对时间表达；切换限额视图或发生自动回退时同步切换，并重新安排该窗口到期时的立即刷新。隐藏窗口的重置时刻不触发立即刷新。
_Avoid_: 重置日期、重置时间
